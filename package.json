{
  "name": "researchmap-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260727.1",
    "wrangler": "^4.114.0"
  }
}
